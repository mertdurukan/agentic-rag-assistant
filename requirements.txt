# =============================================================================
# Agentic RAG Assistant — bağımlılıklar
# Versiyonlar pinlendi: hızlı gelişen kütüphanelerde (LangGraph/RAGAS/Langfuse)
# API kayması yaşamamak için. Güncellerken testleri çalıştır.
#
# Alt sınırlar (>=) bilerek dar tutuldu: pip'in çözücüsü en yeni sürümden
# geriye doğru yürüdüğü için gevşek alt sınırlar tüm langchain-* sürüm
# geçmişinin taranmasına ve "resolution-too-deep" hatasına yol açıyordu.
# Alt sınırı bilinen-çalışan sürüme çekmek bu taramayı kesiyor.
# =============================================================================

# --- Orchestration / LLM ---
# langgraph 1.1+ artık langchain-core>=1.0 istiyor; aşağıdaki 0.3 pini ile
# uyumsuz. Bu yüzden üst sınır <1.1 (eski <2.0 pratikte ulaşılamazdı).
langgraph>=1.0.1,<1.1
langchain-core>=0.3.86,<0.4
langchain-text-splitters>=0.3.11,<0.4
langchain-huggingface>=0.2.0,<0.3
langchain-ollama>=0.3.10,<0.4
langchain-openai>=0.3.35,<0.4
langchain-anthropic>=0.3.22,<0.4

# ragas bunları sürüm kısıtı olmadan çekiyor; 1.x majörleri langchain-core>=1.0
# istediği için açıkça 0.3 serisine sabitlendi.
langchain>=0.3.30,<0.4
langchain-community>=0.3.31,<0.4

# --- Vektör DB ---
# langchain-postgres 0.0.x serisi minor sürümler arasında breaking değişiklik
# yapabiliyor; üst pin kayma riskini sınırlar.
langchain-postgres>=0.0.17,<0.1
psycopg[binary]>=3.3,<4
pgvector>=0.3.6,<0.5

# --- Retrieval ---
sentence-transformers>=5.7,<6.0
rank-bm25>=0.2.2

# --- Ingestion ---
arxiv>=2.1

# --- Eval / Observability ---
ragas>=0.3.9,<0.4
langfuse>=3.15,<4.0

# --- API / UI ---
fastapi>=0.110
uvicorn[standard]>=0.27
gradio>=4.44

# --- Config ---
pydantic>=2.6
pydantic-settings>=2.2

# --- Test ---
pytest>=8.0
